{
	"$comment": [
		"Full end-to-end: the real Azure, Octopus and Coralogix adapters, pointed at local",
		"emulators. Only the base URLs differ from a production file — the providers, their",
		"mapping, their paging and their auth are the production code paths.",
		"",
		"  bun run db:up        # Postgres and floci-az",
		"  bun run db:migrate",
		"  bun run seed:azure   # floci-az starts empty; without this the estate is blank",
		"  bun run dev:stack    # Octopus, Coralogix and the Azure cost mock",
		"",
		"  SOURCES_CONFIG=sources.local.json \\",
		"  DATABASE_URL=postgres://postgres:postgres@localhost:5432/command_center \\",
		"  bun --bun node_modules/.bin/vite dev",
		"",
		"No SOURCES_ALLOW_FIXTURES here, and no fixture-cloud: every kind has a real",
		"adapter now, and loadConnections would refuse a fixture beside one anyway.",
		"",
		"Two Azure surfaces have no local backing and are stated gaps rather than faked:",
		"Cost Management, which floci-az does not emulate and our own mock serves on 4593,",
		"and Service Bus, which it refuses to provision through ARM at all."
	],
	"connections": [
		{
			"id": "azure-local",
			"provider": "azure",
			"label": "Azure (floci-az)",
			"settings": {
				"baseUrl": "http://localhost:4577",
				"costBaseUrl": "http://localhost:4593",
				"subscriptionId": "00000000-0000-0000-0000-000000000001",
				"tenantId": "00000000-0000-0000-0000-000000000002",
				"clientId": "local",
				"clientSecret": { "$env": "AZURE_LOCAL_SECRET" }
			}
		},
		{
			"id": "octopus-local",
			"provider": "octopus",
			"label": "Octopus (mock)",
			"settings": {
				"baseUrl": "http://localhost:4591",
				"apiKey": "local-dev-key",
				"spaceId": "Spaces-1",
				"windowSize": 400
			}
		},
		{
			"id": "coralogix-local",
			"provider": "coralogix",
			"label": "Coralogix (mock)",
			"settings": { "baseUrl": "http://localhost:4592", "apiKey": "local-dev-key" }
		}
	]
}
